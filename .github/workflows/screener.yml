name: Smart Money Screener

on:
  schedule:
    - cron: '0 20 * * 0'
    - cron: '0 13 * * 1-5'
    - cron: '45 13 * * 1-5'
    - cron: '30 21 * * 1-5'

  workflow_dispatch:
    inputs:
      phase:
        description: 'Faza de rulat'
        required: true
        default: 'scan'
        type: choice
        options:
          - universe
          - scan
          - enrich

jobs:
  run-screener:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Detecteaza faza
        id: detect-phase
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "phase=${{ github.event.inputs.phase }}" >> $GITHUB_OUTPUT
          else
            HOUR=$(date -u +%H)
            MIN=$(date -u +%M)
            DOW=$(date -u +%u)

            if [ "$DOW" = "7" ] && [ "$HOUR" = "20" ]; then
              echo "phase=universe" >> $GITHUB_OUTPUT
            elif [ "$HOUR" = "13" ] && [ "$MIN" -lt "30" ]; then
              echo "phase=scan" >> $GITHUB_OUTPUT
            else
              echo "phase=enrich" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Ruleaza faza
        run: python collectors/run.py --phase ${{ steps.detect-phase.outputs.phase }}
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
          FMP_KEY: ${{ secrets.FMP_KEY }}
